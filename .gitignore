# ===================== Visual Studio 相关（你的环境专属） =====================
.vs/
obj/
*.vsconfig
# VS 解决方案缓存
*.sln.docstates
*.suo
*.user
*.userosscache
*.sln.iml
# ===================== UE 项目核心排除项（你的目录清单适配） =====================
# 编译/缓存目录
bin/
Binaries/
Build/
DerivedDataCache/
Intermediate/
# 本地保存/日志
Saved/
# 打包产物
package/
# ===================== Config 目录：仅排除本地配置，保留核心配置 =====================
# 排除本地编辑器配置（每个人的设置不同）
Config/DefaultEditor.ini
Config/DefaultEditorPerProjectUserSettings.ini
Config/Input.ini
# 保留 Config 目录其他文件（核心游戏配置）
!Config/
# ===================== 通用排除项 =====================
# 操作系统临时文件
.DS_Store       # Mac系统（如果团队有Mac成员保留）
Thumbs.db       # Windows系统
desktop.ini
# 日志/临时文件
*.log
*.tmp
*.bak
*.backup
# Crash报告
CrashReportClient/